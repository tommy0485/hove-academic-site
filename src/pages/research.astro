---
import BaseLayout from '@/components/BaseLayout.astro';
import ResearchInterest from '@/components/ResearchInterest.astro';
import publications from '@/data/publications.json';
import profile from '@/data/profile.json';

const meta = {
	title: 'Research',
	description: `Research interests and current directions for ${profile.displayName}.`,
};
---

<BaseLayout {meta}>
	<article class="prose dark:prose-invert max-w-none">
		<header class="mb-10">
			<h1 class="text-4xl font-extrabold text-gray-900 dark:text-gray-50 border-b pb-4 border-gray-200 dark:border-gray-700">
				🔬 Research
			</h1>
			<p class="mt-2 text-xl text-gray-600 dark:text-gray-300">
				Overview of my primary research focus areas and ongoing work.
			</p>
		</header>

		<section class="mb-12">
			<h2 class="text-3xl font-bold mb-6 text-red-600 dark:text-red-400">Current Interests & Expertise</h2>
			<ul class="grid grid-cols-1 md:grid-cols-2 gap-4" role="list">
				{profile.researchInterests.map((interest) => (
					<ResearchInterest interest={interest} role="listitem" />
				))}
			</ul>
		</section>

		<section class="mb-12 p-6 bg-gray-50 dark:bg-gray-800 rounded-lg shadow-lg">
			<h2 class="text-3xl font-bold mb-4 text-red-600 dark:text-red-400">Focus: Time-Series Prediction for Environmental Modeling</h2>
			<p class="text-lg text-gray-700 dark:text-gray-300 leading-relaxed mb-4">
				My primary research focus involves applying **advanced machine learning models**, specifically Long Short-Term Memory (LSTM) and Transformer networks, to complex time-series data. I am particularly interested in **environmental prediction**, such as forecasting **wildfire smoke plume behavior (PM2.5)**, to provide critical, short-term actionable intelligence for public health and environmental agencies.
			</p>
			<p class="text-lg text-gray-700 dark:text-gray-300 leading-relaxed">
				This work involves **large-scale data processing**, **data pipeline optimization**, and the creation of highly accurate predictive models that account for temporal dependencies and non-linear patterns.
			</p>
		</section>

		<section>
			<h2 class="text-3xl font-bold mb-6 text-red-600 dark:text-red-400">Related Publications</h2>
			<p class="mb-4 text-gray-700 dark:text-gray-300">
				Find a complete, filterable list on the <a href="/publications" class="text-red-600 hover:text-red-700 dark:text-red-400 dark:hover:text-red-300 font-medium transition-colors">Publications page</a>.
			</p>

			<ul class="list-disc list-outside space-y-3 text-gray-700 dark:text-gray-300 ml-4">
				{publications.slice(0, 3).map((pub) => (
					<li class="pl-2">
						<a href={pub.url || '/publications'} class="text-red-600 hover:text-red-700 dark:text-red-400 dark:hover:text-red-300 font-medium transition-colors">
							{pub.title}
						</a>. {pub.authors[0]} et al. ({pub.year}). {pub.venue}.
					</li>
				))}
			</ul>
		</section>
	</article>
</BaseLayout>